{
    "rules": {
     	".read": "auth.token.email.matches(/.*@renegadetec.com$/)",
     	".write": "auth.token.email.matches(/.*@renegadetec.com$/)",
        "auditing": {
            ".indexOn": ["eventdate"]
        },
        "users":{
          	".indexOn" : ["email"]
        }
    }
}